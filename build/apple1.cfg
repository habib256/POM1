MEMORY {
    RAM: start = $0300, size = $FCFF, file = %O, fill = no;
}

SEGMENTS {
    CODE: load = RAM, type = rw;
    DATA: load = RAM, type = rw;
    BSS:  load = RAM, type = bss, optional = yes;
    RODATA: load = RAM, type = rw;
}
